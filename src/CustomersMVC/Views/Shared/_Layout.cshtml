<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>
        @Html.ActionLink("Home", "Index", "Home")&nbsp; &nbsp;
        @Html.ActionLink("Customers", "CustomersList", "Home")&nbsp; &nbsp;
    </div>
    <div>
        <p>Something from layout page!</p>
        @RenderBody()
    </div>
</body>
</html>
